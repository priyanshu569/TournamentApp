-- Confirmed unused (per project notes) and safe to drop. Left commented out
-- so this migration is a no-op until you're ready — uncomment both lines and
-- push when convenient.

-- drop function if exists public.get_broadcast_push_tokens;
-- drop function if exists public.get_confirmed_player_tokens;
